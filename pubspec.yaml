name: analytica_assement
description: "Test project for Analytica"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.2.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  get: ^4.6.6
  dartz: ^0.10.1
  google_fonts: 6.0.0
  dio: ^5.7.0
  equatable: ^2.0.7
  connectivity_plus: ^6.0.3
  flutter_dotenv: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Add assets
  assets:
    - assets/locales/
    - env/.env

